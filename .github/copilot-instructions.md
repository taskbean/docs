# GitHub Copilot adapter

`AGENTS.md` is the canonical repository instruction file. Read and follow it before changing documentation, configuration, skills, agents, or automation.

Load the committed `mintlify` skill for Mintlify schema and authoring work. Load `docs-maintenance` for product synchronization, weekly audits, autonomous pull requests, or verifier handoffs. Do not duplicate or override their rules here.
